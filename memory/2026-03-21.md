# 2026-03-21

- Learned/confirmed the `gogcli` skill is usable on this machine via `C:\Users\Ian\AppData\Local\Microsoft\WinGet\Links\gog.exe`.
- Confirmed `gogcli` auth is set up for `clareclaw4@gmail.com` with Gmail access.
- Useful read-only Gmail command: `gog gmail search 'is:unread' --max 5 --json --results-only`.
- Note: the workspace `skills/gogcli` folder currently contains the skill docs only, not the binary itself.
