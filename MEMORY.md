# MEMORY

## Tools / Skills

- Gmail connection is working through `gogcli`, with authenticated access available for `clareclaw4@gmail.com` on this machine.
- `gogcli` is available on this Windows machine via the WinGet link path `C:\Users\Ian\AppData\Local\Microsoft\WinGet\Links\gog.exe`.
- The workspace `skills/gogcli` folder may contain only the skill docs, not the binary itself, so the executable should be verified separately.
- GitHub skill is installed in the workspace at `skills/github/`.
- Self-improving-agent skill is installed in the workspace at `skills/xiucheng-self-improving-agent/`.
