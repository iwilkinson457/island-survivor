# MEMORY

## Tools / Skills

- `gogcli` is available on this Windows machine via the WinGet link path `C:\Users\Ian\AppData\Local\Microsoft\WinGet\Links\gog.exe`.
- The `gogcli` skill docs may exist in the workspace without bundling the actual binary, so verify the executable separately.
- Gmail can be checked read-only with `gog gmail search 'is:unread' --max 5 --json --results-only` when authentication is already configured.
